{
  "name": "hm-newsadmin",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "ciAll": "npm ci; npm --prefix client ci && npm --prefix server ci",
    "postinstall": "npm --prefix client install && npm --prefix server install",
    "dev": "npm run --prefix client dev",
    "test": "npm run --prefix client test",
    "start": "npm run --prefix server start",
    "build:client": "npm run --prefix client build",
    "build:server": "npm run --prefix server build",
    "build": "run-p build:*",
    "clean:client": "npm run --prefix client clean",
    "clean:server": "npm run --prefix server clean",
    "clean": "run-p clean:*",
    "prettier": "npm run --prefix client prettier",
    "lint": "npm run --prefix client lint",
    "knip": "knip"
  },
  "dependencies": {
    "@types/umami": "^2.10.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "knip": "^6.15.0",
    "npm-run-all": "^4.1.5",
    "openapi-typescript": "^7.13.0",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3"
  }
}
